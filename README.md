# Staging branch: Windows on ARM wheelhouse builds

Disposable. Two workflows build the win_arm64 wheels the Unsloth WoA installer needs (pyarrow from the Arrow release tag, sqlite-vec from its release tag) on the hosted windows-11-arm runner. Never merged.
